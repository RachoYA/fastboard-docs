# Свободный режим SQL-редактора | Документация Fastboard

Source: https://help.fastboard.online/user/osobennosti-sql-redaktora/svobodnyi-rezim-sql-redaktora/

**Свободный режим SQL-редактора** — это расширенная среда для написания запросов, предназначенная для опытных аналитиков и разработчиков, глубоко знающих SQL и специфику СУБД ClickHouse. Режим снимает ограничения в стандартном редакторе SQL, предоставляя полный контроль над структурой и логикой SQL-запроса.


## **Ключевые особенности и отличия от стандартного режима**[​](#ключевые-особенности-и-отличия-от-стандартного-режима)

**Полный контроль над запросом**

- **Отсутствие автоматической обёртки:** Система не добавляет конструкции `SELECT ... FROM`. Вы пишете запрос целиком, начиная с `WITH` (CTE) или `SELECT`.
- **Произвольная структура:** Доступно использование любых SQL-конструкций: `WITH` (CTE), вложенные подзапросы в любой части запроса, сложные `JOIN`.
- **Прямая работа с функциями ClickHouse:** Полный доступ ко всем встроенным и агрегационным функциям ClickHouse без каких-либо ограничений.
- **Возможность использования любых оконных функций:** доступны все функции предусмотренные ClickHouse. Их использование не ограничено отсутствием возможности применения `DISTINCT` в секции `SELECT`.
- **Возможность использования функционала массивов:** поддерживается весь функционал массивов `ARRAY` и функций для работы с ними.
- **Ограничение вывода результатов запроса:** свободное использование `LIMIT` и `OFFSET`.
**Предназначение для сложных задач**

- Режим идеально подходит для реализации комплексной бизнес-логики, многоэтапных расчётов, прототипирования запросов для дашбордов (отчётов) и глубокой аналитики данных, требующей максимальной гибкости.
**Требования к пользователю**

- **Обязательное знание SQL (язык структурированных запросов):** Пользователь должен самостоятельно обеспечивать корректность синтаксиса и логики запроса.
- **Понимание модели данных:** Необходимо точно знать структуру таблиц, имена полей и типы данных, так как автоматическая подстановка полей из "Карты модели" может быть ограничена в зависимости от сложности написанного запроса.
- **Ответственность за производительность:** Пользователь самостоятельно оптимизирует запрос, учитывая особенности ClickHouse. Некорректные или тяжелые запросы могут влиять на производительность системы.
**Ограничения режима**

- **Только `SELECT` запросы:** запрещены запросы `INSERT`, `DELETE`, `DROP` и иные в целях безопасности хранилища данных. Помимо этого, обращаться в запросе можно только к базе данных, связанной с текущим проектом.

### **Общий вид расширенного SQL редактора**[​](#общий-вид-расширенного-sql-редактора)

Переключиться в **Свободный режим SQL** можно при создании или редактировании показателей через стандартный SQL-редактор. Система запросит подтверждение, так как после активации этого режима возврат к стандартному интерфейсу для данного показателя будет невозможен без переписывания логики.

![Переключиться в Свободный режим SQL можно при создании или редактировании показателей через стан](https://help.fastboard.online/assets/images/открытие-окна-редактора-c3a32180183be77633a549f623e00b20.png)

> **Со скриншота** ([изображение](https://help.fastboard.online/assets/images/открытие-окна-редактора-c3a32180183be77633a549f623e00b20.png)):
> 1)  
> Редактор Кода  
> Свободный режим  
> Выражение  
> SELECT  
> Выражение для разрезов  
> |  
> Выражение для показателей  
> |  
> Опишите группировки и фильтры  
> |  
> Переход в режим свободного редактирования  
> Вы собираетесь перейти в режим свободного редактирования.  
> В этом режиме Вы сможете использовать все возможности SQL для получения данных из модели, но выбор данных из настроек в интерфейсе будет невозможен  
> Отмена  
> Продолжить  
> Разрезы:  
> Группа  
> Показатели:  
> Показатель 0  
> Показатель 1  
> Форматировать  
> Отменить  
> Сохранить  
> 2) Скриншот показывает диалоговое окно подтверждения перехода в режим свободного редактирования SQL-запросов в BI-платформе Fastboard, с предупреждением о невозможности выбора данных из интерфейса после активации этого режима.


<https://book.winsolutions.ru/uploads/images/gallery/2025-12/V46image.png>В свободном режиме, как и в стандартном, осуществляется проверка синтаксиса запроса. В случае выявлении ошибки появляется информация о самой ошибке, а также в интерфейсе подсвечивается место, где допущена ошибка.

![В свободном режиме,](https://help.fastboard.online/assets/images/снимок-экрана-2026-01-27-в-12.16.23-fbe347dbf1e0732effa882fed7c15b3d.png)

> **Со скриншота** ([изображение](https://help.fastboard.online/assets/images/снимок-экрана-2026-01-27-в-12.16.23-fbe347dbf1e0732effa882fed7c15b3d.png)):
> 1)  
> Редактор Кода  
> Карта модели  
> Поиск  
> fact  
> calendar  
> Свободный режим  
> Выражение  
> 1 SELECT  
> 2 fact.Filial AS "Подразделения",  
> 3 count(Registratsionnyy_nomer_SED) AS "Показатель 0"  
> 4 FaROM  
> 5 fact AS fact  
> 6 LEFT JOIN calendar AS calendar ON `calendar`.date_id = `fact`.date_id  
> 7 WHERE  
> 8 calendar.offset = '0'  
> 9 AND fact.entity = 'Жалобы'  
> 10 GROUP BY  
> 11 fact.Filial  
> Добавить в текст  
> Форматировать  
> Отменить  
> Сохранить  
> ОШИБКИ СИНТАКСИСА SQL  
> FREE MODE SQL  
> Expected "#", ",", "-", "/*", ";",  
> "COLLATE", "EXCEPT", "FOR", "FROM",  
> "GO", "GROUP", "HAVING",  
> "INTERSECT", "INTO", "LIMIT", "LOCK",  
> "MINUS", "ORDER", "UNION", "WHERE",  
> "WINDOW", [\t\n\r], or end of input but  
> "F" found.
> 2) Скриншот показывает редактор SQL-запросов в BI-платформе Fastboard с ошибкой синтаксиса из-за опечатки «FaROM» вместо «FROM».


<https://book.winsolutions.ru/uploads/images/gallery/2025-12/J6zimage.png>В случае, если Вы допустили ошибку, либо не закончили писать Ваш запрос, есть возможность сохранить результат и вернуться к редактированию позже.

Когда написание запроса окончено без ошибок, в правой части интерфейса появится автоматическое разделение результата выполнения запроса на разрезы и показатели.

![Когда написание запроса окончено без ошибок, в правой части интерфейса появится автоматическое р](https://help.fastboard.online/assets/images/разрезы-и-показатели-sql-c899ddee859bad5ea7e652c6e4da728d.png)

> **Со скриншота** ([изображение](https://help.fastboard.online/assets/images/разрезы-и-показатели-sql-c899ddee859bad5ea7e652c6e4da728d.png)):
> 1)  
> Редактор Кода  
> Карта модели  
> Поиск  
> fact  
> calendar  
> Свободный режим  
> Выражение  
> SELECT  
>   fact.Filial AS "Подразделения",  
>   count(Registratsionnyy_nomer_SED) AS "Показатель 0"  
> FROM  
>   fact AS fact  
>   LEFT JOIN calendar AS calendar ON `calendar`.`date_id` = `fact`.`date_id`  
> WHERE  
>   calendar.offset = '0'  
>   AND fact.entity = 'Жалобы'  
> GROUP BY  
>   fact.Filial|  
> Разрезы:  
> Подразделения  
> Показатели:  
> Показатель 0  
> Добавить в текст  
> Форматировать  
> Отменить  
> Сохранить  
> 2) Скриншот показывает интерфейс редактора SQL-запросов в BI-платформе Fastboard с активным свободным режимом, где пользователь редактирует запрос для агрегации данных по подразделениям и количеству регистрационных номеров SED с фильтром по дате и типу записи «Жалобы», а также отображает выбранные разрезы и показатели.


<https://book.winsolutions.ru/uploads/images/gallery/2025-12/u9simage.png>


### **Карта модели**[​](#карта-модели)

Для упрощения работы в расширенный редактор встроена **карта модели данных**, аналогичная стандартному редактору:

- Отображает все доступные поля таблиц;
- Оснащена **поиском**;
- Позволяет вставить нужное поле в текст запроса одним кликом.
[![image.png](https://book.winsolutions.ru/uploads/images/gallery/2025-12/scaled-1680-/l5aimage.png)

> **Со скриншота** ([изображение](https://book.winsolutions.ru/uploads/images/gallery/2025-12/scaled-1680-/l5aimage.png)):
> Карта модели  
> Поиск  
> sales  
> category_id  
> customer_id  
> discount  
> event_date  
> event_timestamp  
> is_promotional  
> product_id  
> Скриншот показывает интерфейс раздела «Карта модели» в BI-платформе Fastboard с поисковой строкой и списком полей таблицы sales, каждое из которых имеет чекбокс для выбора.
](https://book.winsolutions.ru/uploads/images/gallery/2025-12/l5aimage.png)


### **Взаимодействие с фильтрами дашборда**[​](#взаимодействие-с-фильтрами-дашборда)

Если у фильтра в настройках включено влияние на визуальный компонент, созданный с помощью расширенного редактора SQL, то условие фильтрации будет добавлено в SQL запрос в секцию `WHERE` основной части запроса автоматически. На это стоит обращать внимание, например, если в своем запросе пользователь не использует таблицу, по которой осуществляется фильтрация, это приведет к ошибке.

Пример, показывающий ошибочный запрос в результате фильтрации по таблице **sales**, которая не используется в самом запросе:


```
WITH  test_cte AS (    (      SELECT        456 AS y    )  )SELECT  x.x as x,  y.y as yFROM  (    SELECT      arrayJoin ([123, 789]) AS x  ) AS x  CROSS JOIN test_cte AS yWHERE  sales.event_date BETWEEN '2025-01-07' AND '2025-01-08'
```

В таком случае стоит отключать влияние фильтра на данный визуальный компонент.


### **Пример использования**[​](#пример-использования)

В **стандартном режиме** Вы вводите только выражение для поля, например:


```
sumIf(amount, event_type = 'purchase')
```

В **Свободном режиме** Вы создаёте полноценный запрос, например:


```
WITH user_sessions AS (    SELECT        user_id,        session_id,        sumIf(amount, event_type = 'purchase') as revenue,        max(created_at) as last_event    FROM events    WHERE created_at >= today() - 7    GROUP BY user_id, session_id)SELECT    us.user_id as user_id,    count() as sessions_count,    sum(us.revenue) as total_revenue,    any(users.segment) as user_segmentFROM user_sessions usLEFT JOIN dim_users users ON us.user_id = users.idWHERE revenue > 0GROUP BY us.user_idORDER BY total_revenue DESC
```


### **Особенности использования с разными типами визуальных компонентов**[​](#особенности-использования-с-разными-типами-визуальных-компонентов)

**Круговые, линейный и столбчатые диаграммы.** Важен порядок указания разрезов и показателей в основной секции запроса.


```
SELECT  category_id as category,  product_id as product,  SUM(revenue) as sum_revenue,  count(*) as total_rowsFROM  salesGROUP BY  category_id,  product_id
```

Данный пример создаст два разреза - по категории и по товару. Разрез идентифицируется системой по наличию поля в секции `GROUP BY`. Также, будут созданы два показателя - сумма продаж, и кол-во заказов(кол-во строк в таблице). Если изменить порядок разрезов, это может привести к нарушению логики Drill-Down, когда пользователь будет сначала выбирать товар, а затем видеть категорию товара.

**Таблицы.** На данный момент существует два ограничения применения свободного режима SQL в данных видах визуальных компонентов: отсутствует возможность выбора разреза в шапке таблицы и группировки в виде раскрывающихся строк. Визуально в таблице разрезы и показатели выводятся в том же порядке, что и в основной части запроса SQL.

**Диаграмма Ганта**. На данный момент в виду специфики данного типа визуальных компонентов, свободный режим SQL-редактора не доступен.


### **Важное примечание**[​](#важное-примечание)

Используйте этот режим для реализации максимально сложной и специфической логики, когда возможностей стандартного редактора SQL недостаточно.
